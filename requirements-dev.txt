# Local development and testing dependencies.
# Install with: pip install -r requirements-dev.txt
# Does NOT get deployed to Lambda; per-Lambda requirements.txt files handle that.

# AWS SDK
boto3>=1.34

# Required by python-ulid>=2.0
typing_extensions>=4.0

# Redis
redis>=5.0

# X-Ray
aws-xray-sdk>=2.12

# ID generation
python-ulid>=2.0

# AWS mocking (all services used by this project)
moto[dynamodb,kinesis,sns,sqs]>=5.0

# Fake Redis for unit tests (avoids needing a running Docker Redis for unit tests)
fakeredis>=2.20

# Testing framework
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.0

# Load testing (Zhuoqun uses this for simulator/tests/locustfile.py)
locust>=2.24

# Code quality
black>=24.0
flake8>=7.0
